/**
 * lib.site-container (사이트 공통 컨테이너)
 * =======================================
 * 사이트 container-width(1400) + xl/2xl 여백(px-20/24) 점진 확장
 *
 * [Main Functions]
 * - siteContainerClass
 *
 * [Dependencies]
 * - 없음
 */

// 1. siteContainerClass
export const siteContainerClass =
  'mx-auto w-full max-w-[1400px] px-6 sm:px-8 lg:px-12 xl:max-w-[1600px] xl:px-20 2xl:max-w-[1720px] 2xl:px-24';
