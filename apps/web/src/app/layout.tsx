/**
 * app.layout (루트 레이아웃)
 * ==========================
 * next-intl static export: html/body는 [locale]/layout.tsx에서 locale별 lang 설정
 *
 * [Main Functions]
 * - RootLayout
 *
 * [Dependencies]
 * - next
 */

// 1. RootLayout
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return children;
}
